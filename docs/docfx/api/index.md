# FunctionalExtensions API Reference

This directory is generated by DocFX from the `FunctionalExtensions/FunctionalExtensions.csproj` metadata. Do not edit these files manually—run the DocFX pipeline instead:

```bash
cd docs/docfx
docfx metadata
docfx build
```

The generated site (under `_site/`) includes:
- Namespaces, types, and extension members emitted from XML documentation.
- Cross-links back to conceptual, how-to, and sample articles under `docs/`.

See `docs/docfx/README.md` for detailed instructions on configuring the toolchain.
